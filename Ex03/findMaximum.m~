function output = findMaximum( input )
%FINDMAXIMUM 此处显示有关此函数的摘要
%   此处显示详细说明

[row, col] = size(input);
output = zeros(1,3);
max = 0;
for i = 1 : row
    for i = 1 : col
        if input(i,j) >= max
            max = input(i,j);
            output = [i, j, max];

end

